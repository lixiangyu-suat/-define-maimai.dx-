#include <bits/stdc++.h>
using namespace std;

const int N = 3e5+5;
int n;
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(0);

    cin >> n;

    for (int i = 0; i < n; i++);

    return 0;
}